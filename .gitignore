# Binaries (build locally, or grab them from Releases)
sm-unlocker-linux
sm-unlocker-windows.exe
*.exe

# Go build cache / test artifacts
/bin/
*.test
*.out

# Release staging / checksums
/dist/
sha256sums.txt
